spring.application.name=secure-messaging-system
server.port=${PORT:8080}
# =========================
# PostgreSQL Configuration
# =========================

spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver


# =========================
# JPA / Hibernate
# =========================

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# =========================
# Flyway Database Migrations
# =========================

spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true
spring.flyway.baseline-version=0
spring.flyway.validate-on-migrate=true


# =========================
# JWT
# =========================

app.jwt.secret=${JWT_SECRET:securemessagingjwtsecretkeymustbeatleast32chars}

# =========================
# File Upload Limits
# =========================

spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

# =========================
# Advisory Attention Classification
# =========================

app.ai.attention.enabled=${AI_ATTENTION_ENABLED:false}
app.ai.attention.url=${AI_ATTENTION_URL:http://127.0.0.1:8090}
app.ai.attention.api-key=${AI_INTERNAL_API_KEY:}
app.ai.attention.threshold=${AI_ATTENTION_THRESHOLD:0.50}
